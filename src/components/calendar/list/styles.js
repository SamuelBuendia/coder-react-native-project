import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  listContainer: {
    height: "45%",
    width: "100%",
    backgroundColor: '#E6E6E6',
    marginTop: 20,
    padding: 10,
  }
})
